function login(event) {
    if (!event.ctrlKey) {
        document.getElementById("loginForm").submit();
    } else {
        alert(ESAPI.encoder().encodeForJavaScript(request.getParameter("' OR '1'='1")));
    }

}


